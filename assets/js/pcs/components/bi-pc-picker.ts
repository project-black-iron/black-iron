import { css, html, LitElement } from "lit";
import { customElement, property } from "lit/decorators.js";

@customElement("bi-pc-picker")
export class BiPCPicker extends LitElement {
  static styles = css`
  `;

  @property()
  text?: string;

  render() {
    return html`

    `;
  }
}
