{
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && eslint js && node esbuild.config.js production",
    "build:dev": "tsc --noEmit && eslint js && node esbuild.config.js nowatch",
    "dev": "node esbuild.config.js",
    "test": "borp --coverage"
  },
  "devDependencies": {
    "@jgoz/esbuild-plugin-typecheck": "^4.0.1",
    "@types/node": "^20.14.12",
    "@types/phoenix": "^1.6.5",
    "borp": "^0.17.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.7.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
